<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:samples="clr-namespace:Samples"
             x:DataType="samples:UserDialogsViewModel"
             x:Class="Samples.UserDialogsPage"
             Title="User Dialogs">

    <ContentPage.Content>
        <CollectionView ItemSizingStrategy="{Binding Actions}"
                        SelectedItem="{Binding }">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="samples:ActionItemViewModel">
                    <StackLayout>
                        <Label Text="{Binding Text}" FontSize="Large" />
                        <Label Text="{Binding Detail}" FontSize="Medium" FontAttributes="Italic" />
                        <BoxView HeightRequest="1"
                                 BackgroundColor="Black"
                                 VerticalOptions="Center"
                                 HorizontalOptions="FillAndExpand" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>